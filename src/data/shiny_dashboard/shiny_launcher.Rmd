---
title: Shiny dashborad
author: "FSA team"
output:
  html_document: default
  html_notebook: default
  pdf_document:
    latex_engine: xelatex
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(warning = FALSE, echo=FALSE)
```
```{r,message=FALSE,warning=FALSE,echo=FALSE}
library(RPostgreSQL)
library(tidyverse)
library(dbplyr)
library(rjson)
library(DBI)
library(lubridate)
library(tibble)
library(ggplot2)
library(stringr)
library(gridExtra)
library(network)
library(ggraph)
library(visNetwork)
library(networkD3)
library(igraph)
library(tidygraph)
library(cluster) 
library(fpc)
library(plotly)
library(cluster)
library(factoextra)
#library(anomalize)
library(boot)
library(leaps)
library(shiny)
#library(DT)
```

# Load the data: all_info, si
```{r}
load('all_shiny_data.RData')
```


# ```{r}
# source("model_get_data.R")
# pathname <- "../"
# filename <- "Comtrade_all.csv"
# route    <- paste(pathname,filename,sep="")
# data1    <- model_get_data(route)
# all_info <- data1$all_info
# si       <- data1$si
# list_of_countries <- sort(unique(all_info$node))
# ```

#```{r}
#source("model_kmeans.R")
#testing <- model_kmeans(all_info,20,"United Kingdom")
#```
#
#```{r}
#source("model_linear.R")
#testing1 <- model_linear(all_info,5,5,0,100,4,0.6,0.5)
#```
#
#```{r}
#source("build_network.R")
#testing1 <- build_network(si,as.Date("2014-01-01"))
#```


# Launch the app
```{r}
runApp("./")
```
