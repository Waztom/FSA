---
title: Shiny dashborad
author: "FSA team"
output:
  html_document: default
  html_notebook: default
  pdf_document:
    latex_engine: xelatex
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(warning = FALSE, echo=FALSE)
```
```{r,message=FALSE,warning=FALSE,echo=FALSE}
library(RPostgreSQL)
library(tidyverse)
library(dbplyr)
library(rjson)
library(DBI)
library(lubridate)
library(tibble)
library(ggplot2)
library(stringr)
library(gridExtra)
library(network)
library(ggraph)
library(visNetwork)
library(networkD3)
library(igraph)
library(tidygraph)
library(cluster) 
library(fpc)
library(plotly)
library(cluster)
library(factoextra)
library(anomalize)
library(boot)
library(leaps)
library(shiny)
library(btergm)
```

```{r}
cucumber_model <- readRDS("cucumber_network_model_si.rds")
```

#This is the pre-load.
#As it is right now is horrifying and should be replaced by RData
```{r}
source("model_get_data.R")
data        <- model_get_data("Vanilla")
all_info_va <- data$all_info
si_va       <- data$si

data        <- model_get_data("Cucumbers")
all_info_cu <- data$all_info
si_cu    <- data$si

data        <- model_get_data("Beer")
all_info_be <- data$all_info
si_be    <- data$si

data        <- model_get_data("Milk")
all_info_mi <- data$all_info
si_mi    <- data$si

data        <- model_get_data("Maple Syrup")
all_info_ms <- data$all_info
si_ms    <- data$si
```

# Launch the app
```{r}
runApp("./")
```

